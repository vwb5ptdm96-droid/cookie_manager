# 第二阶段重构设计：以健康检测任务为核心

## 概述

取消独立"维护任务"模块，将检测 + 调度 + 修复合并为"健康检测任务"作为系统核心。引入脚本执行实例（ScriptRun）支持运行跟踪、暂停/继续/取消、去重锁和详细日志。

---

## 一、数据模型变更

### 1. 新增模型：HealthTask（替代 SessionMaintenanceTask + HealthCheckConfig）

```python
# backend/app/models/health_task.py

class HealthTask(Base):
    __tablename__ = "health_task"

    id: int = Column(Integer, primary_key=True, autoincrement=True)
    health_task_code: str = Column(String(64), unique=True, nullable=False)
    health_task_name: str = Column(String(128), nullable=False)
    enabled: bool = Column(Boolean, nullable=False, default=True)

    # ── 1. 检测配置 ──
    cookie_table: str = Column(String(128), nullable=False, default="ods_cookie_playwright")
    channel: str = Column(String(32), nullable=False)
    shop_name: str | None = Column(String(128), nullable=True)
    mobile_phone: str | None = Column(String(32), nullable=True)
    dns: str | None = Column(String(128), nullable=True)
    check_url: str = Column(String(500), nullable=False)
    http_method: str = Column(String(16), nullable=False, default="GET")
    http_headers: str | None = Column(Text, nullable=True)       # JSON
    http_body: str | None = Column(Text, nullable=True)           # JSON
    success_rule: str | None = Column(Text, nullable=True)        # JSON
    failure_rule: str | None = Column(Text, nullable=True)        # JSON

    # ── 2. 高级调度配置 ──
    cron_expression: str | None = Column(String(128), nullable=True)
    check_timeout_seconds: int = Column(Integer, nullable=False, default=30)
    retry_count: int = Column(Integer, nullable=False, default=0)
    last_checked_at: datetime | None = Column(DateTime, nullable=True)
    next_run_at: datetime | None = Column(DateTime, nullable=True)

    # ── 3. 失败修复配置 ──
    auto_repair_enabled: bool = Column(Boolean, nullable=False, default=False)
    repair_script_id: int | None = Column(Integer, nullable=True)           # FK to script_registry.id
    repair_directory_id: int | None = Column(Integer, nullable=True)        # FK to profile_registry.id
    repair_run_mode: str | None = Column(String(16), nullable=True)         # "HEADLESS" / "HEADED", 覆盖脚本默认
    repair_script_config: str | None = Column(Text, nullable=True)          # JSON
    repair_timeout_seconds: int = Column(Integer, nullable=False, default=600)

    # ── 状态 ──
    status: str = Column(String(32), nullable=False, default="PENDING")     # PENDING / PASS / FAIL / DISABLED
    last_run_status: str | None = Column(String(32), nullable=True)         # SUCCESS / FAIL / RISK
    last_result_message: str | None = Column(Text, nullable=True)
    last_repaired_at: datetime | None = Column(DateTime, nullable=True)
    last_repair_run_id: str | None = Column(String(64), nullable=True)      # 最近一次修复的 run_id

    created_at: datetime = Column(DateTime, nullable=False, server_default=func.now())
    updated_at: datetime = Column(DateTime, nullable=False, server_default=func.now(), onupdate=datetime.now)
```

**迁移计划：**
1. `SessionMaintenanceTask` + `HealthCheckConfig` 数据合并插入到 `health_task`
2. 旧表保留但标记废弃，后续清理

**对照映射：**

| HealthCheckConfig 字段 | HealthTask 字段 |
|---|---|
| check_name | health_task_name |
| cookie_table | cookie_table |
| channel | channel |
| shop_name | shop_name |
| mobile_phone | mobile_phone |
| dns | dns |
| method | http_method |
| check_url | check_url |
| request_headers | http_headers |
| request_body | http_body |
| success_rule | success_rule |
| failure_rule | failure_rule |
| trigger_task_id → task_code → script/profile | → repair_script_id + repair_directory_id |
| status | status |
| last_result_message | last_result_message |
| last_checked_at | last_checked_at |
| enabled | enabled |

| SessionMaintenanceTask 字段 | HealthTask 字段 |
|---|---|
| task_name | —（不再需要独立任务名，健康检测任务名即为名称） |
| channel | channel（已覆盖） |
| mobile_phone | mobile_phone（已覆盖） |
| related_dns | dns（已覆盖） |
| script_code | → repair_script_id |
| profile_key | → repair_directory_id |
| script_config | → repair_script_config |
| schedule_type/value | → cron_expression |
| last_run_status | last_run_status |
| last_run_at | last_checked_at |

### 2. 新增模型：ScriptRun（脚本执行实例）

```python
# backend/app/models/script_run.py

class ScriptRun(Base):
    __tablename__ = "script_run"

    id: int = Column(Integer, primary_key=True, autoincrement=True)
    run_id: str = Column(String(64), unique=True, nullable=False)       # "run_<uuid_hex12>"

    # 关联
    health_task_id: int | None = Column(Integer, nullable=True)         # FK to health_task.id
    health_task_code: str | None = Column(String(64), nullable=True)
    script_id: int = Column(Integer, nullable=False)                    # FK to script_registry.id
    script_code: str = Column(String(64), nullable=False)
    directory_id: int | None = Column(Integer, nullable=True)           # FK to profile_registry.id
    directory_key: str | None = Column(String(64), nullable=True)

    # 运行配置
    run_mode: str = Column(String(16), nullable=False, default="HEADLESS")  # HEADLESS / HEADED
    script_config: str | None = Column(Text, nullable=True)                  # JSON
    timeout_seconds: int = Column(Integer, nullable=False, default=600)

    # 运行时
    status: str = Column(
        String(32), nullable=False, default="PENDING"
    )  # PENDING / RUNNING / PAUSED / CANCELING / CANCELED / SUCCESS / FAIL / RISK
    pid: int | None = Column(Integer, nullable=True)                   # 操作系统进程 PID
    start_time: datetime | None = Column(DateTime, nullable=True)
    end_time: datetime | None = Column(DateTime, nullable=True)
    duration_ms: int | None = Column(Integer, nullable=True)

    # 产物
    artifact_dir: str | None = Column(String(255), nullable=True)
    log_file: str | None = Column(String(255), nullable=True)           # run.log 路径
    stdout_file: str | None = Column(String(255), nullable=True)
    stderr_file: str | None = Column(String(255), nullable=True)
    result_json: str | None = Column(Text, nullable=True)               # result.json 完整内容
    error_message: str | None = Column(Text, nullable=True)
    exit_code: int | None = Column(Integer, nullable=True)

    # 控制
    control_file: str | None = Column(String(255), nullable=True)       # control.json 路径

    created_at: datetime = Column(DateTime, nullable=False, server_default=func.now())
    updated_at: datetime = Column(DateTime, nullable=False, server_default=func.now(), onupdate=datetime.now)
```

### 3. ScriptRegistry 增加字段

```python
# backend/app/models/script_registry.py — 新增字段

class ScriptRegistry(Base):
    # ... 已有字段保持不变 ...

    # 新增运行控制字段
    default_run_mode: str | None = Column(String(16), nullable=True, default="HEADLESS")  # HEADLESS / HEADED
    supports_pause: bool = Column(Boolean, nullable=False, default=False)
    supports_cancel: bool = Column(Boolean, nullable=False, default=True)
    default_timeout_seconds: int = Column(Integer, nullable=False, default=600)
```

### 4. ProfileRegistry 增加目录锁增强

```python
# 现有 is_locked / lock_owner 已存在，复用
# 新增字段：

class ProfileRegistry(Base):
    # ... 已有字段 ...

    lock_run_id: str | None = Column(String(64), nullable=True)    # 哪个 run_id 持有锁
    locked_at: datetime | None = Column(DateTime, nullable=True)
```

### 5. ManualRepairTicket 增加关联字段

```python
# backend/app/models/repair_ticket.py — 新增字段

class ManualRepairTicket(Base):
    # ... 已有字段 ...

    health_task_id: int | None = Column(Integer, nullable=True)     # FK to health_task.id
    health_task_code: str | None = Column(String(64), nullable=True)
    script_run_id: int | None = Column(Integer, nullable=True)      # FK to script_run.id
    script_run_code: str | None = Column(String(64), nullable=True)
    directory_key: str | None = Column(String(64), nullable=True)   # 冗余，方便展示
```

### 6. 删除旧模型（或标记废弃）

- `SessionMaintenanceTask` — 标记 `__tablename__` 保留，不再使用
- `HealthCheckConfig` — 同上

---

## 二、API 端点设计

### 健康检测任务 `/api/health-tasks`

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/health-tasks` | 列表（含分页/筛选） |
| `POST` | `/health-tasks` | 创建 |
| `GET` | `/health-tasks/{code}` | 详情 |
| `PATCH` | `/health-tasks/{code}` | 编辑 |
| `POST` | `/health-tasks/{code}/toggle` | 启用/停用 |
| `POST` | `/health-tasks/{code}/check` | 立即检测（只检测不修复） |
| `POST` | `/health-tasks/{code}/repair` | 立即执行修复脚本 |
| `POST` | `/health-tasks/check-all` | 全部检测 |

### 脚本执行实例 `/api/script-runs`

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/script-runs` | 列表（含筛选） |
| `GET` | `/script-runs/{run_id}` | 详情 |
| `GET` | `/script-runs/{run_id}/log` | 查看 / 轮询 run.log |
| `GET` | `/script-runs/{run_id}/result` | 查看 result.json |
| `POST` | `/script-runs/{run_id}/pause` | 暂停（写 control.json） |
| `POST` | `/script-runs/{run_id}/resume` | 继续（写 control.json） |
| `POST` | `/script-runs/{run_id}/cancel` | 取消（杀进程 + 写 control.json） |
| `GET` | `/script-runs/running` | 当前运行中的实例列表 |

### 脚本库 `/api/scripts` 新增

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/scripts/{code}/run-config` | 更新运行配置（default_run_mode 等） |

### 目录锁 `/api/profiles`

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/profiles/{key}/lock-status` | 查看目录锁状态 |

---

## 三、核心执行流程

```
健康检测任务（cron 触发 / 手动触发）
  │
  ├─ 1. 执行检测请求
  │     ├─ 检查 cookie 表
  │     ├─ 检测通过 → 更新 status=PASS, last_checked_at
  │     └─ 检测失败 → 进入修复流程
  │
  └─ 2. 修复流程（auto_repair_enabled=true 时自动触发）
        │
        ├─ 2a. 去重检查
        │     检查是否有相同 script_id + directory_id 的 ScriptRun
        │     处于 PENDING / RUNNING / PAUSED / CANCELING
        │     有 → 跳过执行，关联到已有 run_id
        │     无 → 继续
        │
        ├─ 2b. 目录锁检查
        │     检查 directory 是否被锁定
        │     已锁定 → 等待或跳过
        │     未锁定 → 锁定目录
        │
        ├─ 2c. 创建 ScriptRun（status=PENDING）
        │     生成 run_id = "run_{uuid_hex12}"
        │     创建 artifact 目录
        │     写入 config.json
        │     写入 control.json（{pause:false, cancel:false}）
        │
        ├─ 2d. 执行脚本（status=RUNNING）
        │     记录 PID
        │     subprocess 启动 python script.py
        │     捕获 stdout → stdout.log
        │     捕获 stderr → stderr.log
        │     合并写入 run.log
        │
        ├─ 2e. 脚本结束
        │     ├─ 读取 result.json
        │     ├─ SUCCESS → 释放目录锁，更新 health_task 状态
        │     ├─ RISK   → 释放目录锁，创建 RepairTicket
        │     └─ FAIL   → 释放目录锁，更新 health_task 状态
        │
        └─ 2f. 写入运行日志
```

### 暂停/继续/取消机制

```
暂停:
  POST /script-runs/{run_id}/pause
  ├─ 更新 control.json → {"pause": true, "cancel": false}
  ├─ 更新 ScriptRun.status = "PAUSED"
  └─ 记录事件日志

继续:
  POST /script-runs/{run_id}/resume
  ├─ 更新 control.json → {"pause": false, "cancel": false}
  ├─ 更新 ScriptRun.status = "RUNNING"
  └─ 记录事件日志

取消:
  POST /script-runs/{run_id}/cancel
  ├─ 更新 control.json → {"pause": false, "cancel": true}
  ├─ 更新 ScriptRun.status = "CANCELING"
  ├─ 杀进程树（taskkill /T /F /PID {pid}）
  ├─ 释放目录锁
  ├─ 更新 ScriptRun.status = "CANCELED"
  └─ 记录事件日志
```

### 去重键

```
去重判断：script_id + directory_id
- 查询 script_run WHERE script_id=X AND directory_id=Y
  AND status IN (PENDING, RUNNING, PAUSED, CANCELING)
- 有记录 → 跳过执行，关联 health_task.last_repair_run_id = 已有 run_id
- 无记录 → 正常创建新 ScriptRun
```

---

## 四、前端组件变更

### 导航更新

```
旧导航 → 新导航

总览     →  总览（不变）
维护任务 →  删除
健康检测 →  健康检测任务（重构为核心页面）
脚本库   →  脚本库（增加运行配置）
Profile  →  目录库（名称微调）
人工修复 →  人工修复
运行日志 →  运行日志（增加脚本执行详情）
环境自检 →  环境自检
部署配置 →  部署配置
新增     →  脚本运行（独立页面）
```

### 健康检测任务页面（HealthTasksView.vue）— 重构

**三 Tab 配置：**

Tab 1 — 检测配置：
- 检测名称 / cookie 表 / channel / shop_name / mobile_phone / DNS
- 检测 API（URL + 方法 + headers JSON + body JSON）
- 成功规则 JSON / 失败规则 JSON

Tab 2 — 高级调度：
- 启用 / cron 表达式 / 超时时间 / 失败重试次数
- 最近检测时间（只读）/ 下次执行时间（只读）

Tab 3 — 失败修复：
- 启用自动修复开关
- 维护脚本选择（只列出 script_type=MAINTAIN 的脚本）
- 启动目录选择（目录库下拉）
- 运行模式（脚本默认 / HEADLESS / HEADED）
- 脚本配置 JSON
- 执行超时时间

**表格列：**
检测任务名称 | cron 表达式 | cookie 定位信息 | 检测 API | 失败执行脚本 | 启动目录 | 运行模式 | 状态 | 最近检测时间 | 最近修复时间 | 操作

**操作按钮：**
- 编辑 → 打开完整三 Tab 对话框
- 立即检测 → POST /health-tasks/{code}/check
- 执行修复脚本 → POST /health-tasks/{code}/repair
- 查看日志 → 跳转到运行日志（按 health_task_code 筛选）
- 启用/停用 → toggle

### 脚本库页面（ScriptsView.vue）— 增加运行配置列

新增列/可编辑字段：
- default_run_mode：下拉选择 HEADLESS / HEADED
- supports_pause：开关
- supports_cancel：开关
- default_timeout_seconds：数字输入

### 脚本运行页面（ScriptRunsView.vue）— 新增

**统计卡片：**
- 运行中 / 已暂停 / 已完成

**表格列：**
run_id | 健康检测任务 | 脚本 | 启动目录 | 运行模式 | 状态 | 开始时间 | 运行时长 | PID | 操作

**操作按钮：**
- 查看详情 → 弹出详情对话框
- 查看实时日志 → 嵌入或弹窗轮询显示 run.log
- 暂停/继续 → 根据当前状态切换
- 取消 → 杀进程
- 下载 run.log → 后端文件下载
- 查看 result.json → 弹窗展示

**筛选：**
- 状态筛选（PENDING / RUNNING / PAUSED / CANCELING / CANCELED / SUCCESS / FAIL / RISK）
- 健康检测任务筛选

### 运行日志页面（LogsView.vue）— 增加脚本运行筛选

新增筛选条件：
- run_id 筛选
- 关联运行实例列

新增操作：点击"查看脚本日志"跳转到 ScriptRun 详情

### 人工修复页面 — 增加关联展示

处理工单对话框增加：
- 关联健康检测任务信息
- 关联脚本执行实例信息
- 关联脚本 run.log 查看入口

---

## 五、config.json 与 result.json 规范

### config.json（每次执行前生成）

```json
{
  "run_id": "run_xxx",
  "health_task": {
    "id": 1,
    "code": "ht_xxx",
    "name": "快手订单接口检测",
    "channel": "KUAISHOU",
    "shop_name": "快手旗舰店A",
    "mobile_phone": "13800000001",
    "dns": "s.kwaixiaodian.com"
  },
  "script": {
    "script_id": 1,
    "script_code": "script_xxx",
    "script_dir": "D:\\session-maintenance\\runtime\\scripts\\uploaded\\script_xxx\\1.0.0",
    "main_file": "main.py"
  },
  "browser_directory": {
    "directory_id": 1,
    "directory_key": "ks_138_profile",
    "directory_path": "D:\\session-maintenance\\runtime\\profiles\\ks_138_profile"
  },
  "run_mode": "HEADLESS",
  "control_file": "D:\\session-maintenance\\runtime\\artifacts\\runs\\run_xxx\\control.json",
  "artifact_dir": "D:\\session-maintenance\\runtime\\artifacts\\runs\\run_xxx",
  "script_config": {}
}
```

### control.json（前端控制脚本行为）

```json
// 初始状态
{ "pause": false, "cancel": false }

// 暂停
{ "pause": true, "cancel": false }

// 继续
{ "pause": false, "cancel": false }

// 取消
{ "pause": false, "cancel": true }
```

### result.json（脚本执行后输出到 artifact_dir）

```json
// SUCCESS
{
  "status": "SUCCESS",
  "risk": false,
  "message": "执行成功，写入 3 条 cookie",
  "written_rows": 3
}

// RISK
{
  "status": "RISK",
  "risk": true,
  "risk_type": "SMS",
  "message": "出现短信验证"
}

// FAIL
{
  "status": "FAIL",
  "risk": false,
  "message": "脚本执行异常：Chrome 启动失败"
}
```

---

## 六、运行模式优先级

```
健康检测任务 repair_run_mode（显式设置）
  > 脚本库 default_run_mode
  > 系统默认 HEADLESS
```

即：健康检测任务配置的运行模式最高优先级，覆盖脚本库默认和系统默认。

---

## 七、目录锁设计

```
锁定时机：ScriptRun 开始执行前
解锁时机：脚本正常结束 / 取消 / 失败

锁信息：
  profile_registry.is_locked = True
  profile_registry.lock_owner = "run_xxx"
  profile_registry.lock_run_id = "run_xxx"
  profile_registry.locked_at = datetime.now()

去重检测：
  执行前查询是否有该目录的进行中 ScriptRun
  SELECT COUNT(*) FROM script_run
  WHERE directory_id = X
    AND status IN ('PENDING', 'RUNNING', 'PAUSED', 'CANCELING')
  有 → 阻止新执行

锁释放：
  执行后清理 profile_registry 锁字段
```

---

## 八、日志分层

### 事件日志（run_log 表，已有）

记录系统事件的摘要：
- 健康检测开始 / 通过 / 失败
- 开始执行脚本 / 脚本暂停 / 脚本继续 / 脚本取消 / 脚本成功 / 脚本失败 / 脚本风控
- 人工修复开始 / 人工修复完成

### 脚本详细日志（run.log 文件）

```
runtime/artifacts/runs/run_xxx/
├── config.json          # 执行配置
├── control.json         # 控制文件（暂停/取消）
├── run.log              # 完整运行日志（stdout + stderr + 业务日志）
├── stdout.log           # 原始 stdout
├── stderr.log           # 原始 stderr
├── result.json          # 脚本输出结果
├── screenshot/          # 截图目录（可选）
└── trace/               # CDP trace（可选）
```

前端通过 `GET /api/script-runs/{run_id}/log` 轮询查看 run.log 内容（支持 offset 参数实现增量读取）。

---

## 九、实现顺序建议

### Phase 1：数据模型与基础 API（3-4 天）
1. 创建 `HealthTask` 模型 + migration（合并 SessionTask + HealthCheckConfig 数据）
2. 创建 `ScriptRun` 模型 + migration
3. ScriptRegistry 增加字段 migration
4. ProfileRegistry 增加锁字段 migration
5. RepairTicket 增加关联字段 migration
6. 实现 `HealthTaskService` + API routes
7. 实现 `ScriptRunService` + API routes（基础 CRUD）

### Phase 2：执行引擎（2-3 天）
1. 重构 `LocalWindowsExecutor` 支持 PID 跟踪
2. 实现 `control.json` 读写
3. 实现去重检查 + 目录锁
4. 实现暂停/继续（写 control.json）
5. 实现取消（杀进程树 + control.json）
6. 实现 run.log 增量读取 API

### Phase 3：前端重写（3-4 天）
1. 重写 HealthTasksView.vue（三 Tab 配置 + 新列表 + 新操作）
2. 重写 HealthCheckFormDialog → HealthTaskFormDialog
3. 新建 ScriptRunsView.vue（运行实例列表 + 控制操作）
4. 新建 ScriptRunDetailDialog.vue（详情 + 实时日志）
5. ScriptsView.vue 增加运行配置编辑
6. 更新导航
7. 运行日志页增加脚本运行筛选和入口
8. 人工修复页增加关联显示

### Phase 4：整合与测试（2 天）
1. 全链路测试（检测→失败→修复→复检→关闭）
2. 暂停/继续/取消测试
3. 去重锁测试
4. 前端 build 验证
5. 旧模型清理
