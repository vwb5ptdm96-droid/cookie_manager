# 前端模块功能说明

## 导航菜单

| 菜单项 | 路由 | 说明 |
|--------|------|------|
| 总览 | `/` | 系统概览，统计卡片 + 最近日志 + 最近健康检测 |
| 维护任务 | `/tasks` | 维护任务 CRUD、筛选、执行脚本、启停、打开修复浏览器 |
| 健康检测 | `/health-checks` | 健康检测 CRUD、筛选、单条/批量执行、启停 |
| 脚本库 | `/scripts` | 脚本列表、上传、关联 Profile 目录、详情、启停 |
| Profile 目录 | `/profiles` | Profile 目录登记、查看绑定任务和路径预览 |
| 人工修复 | `/repairs` | 工单列表、处理工单（打开浏览器/复检/关闭） |
| 运行日志 | `/logs` | 运行日志多维度筛选和详情查看 |
| 环境自检 | `/environment` | Windows 节点环境自检 |
| 部署配置 | `/deploy` | 部署约束和目录信息展示 |

---

## 1. 总览 Dashboard

**数据加载**：页面加载时自动调用 `GET /api/dashboard`，含 stats、recent_logs、recent_checks。

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 维护任务 | 当前系统中登记的任务总数 |
| Profile 目录 | 当前可管理的浏览器 Profile 数量 |
| 健康检测 | 已配置的登录态检测规则数量 |
| 待人工修复 | 尚未关闭的风控工单数量 |

### 主流程说明

固定的三段式流程描述，无交互。

### 最近日志

展示 `recent_logs` 列表，每条显示：
- title + status（带颜色标签）
- message
- run_type + created_at

空列表时展示 `<el-empty>` 引导文案。

**按钮**：

| 按钮 | 行为 |
|------|------|
| 刷新 | 重新请求 `GET /api/dashboard` 刷新所有数据 |

### 最近健康检测

`<el-table>` 展示 `recent_checks`，列：检测名称 / 检测编码 / 状态（带颜色标签）/ 最近结果 / 最近检测时间。空表时展示引导文案。

---

## 2. 维护任务 SessionTasks

**数据加载**：页面加载时自动请求 `GET /api/tasks`、`GET /api/profiles`、`GET /api/scripts`、`GET /api/repairs`。

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 任务总数 | 全部任务数 |
| 有效任务 | `status === "VALID"` 的任务数 |
| 风险任务 | `status === "RISK"` 的任务数 |

### 筛选栏

| 控件 | 说明 |
|------|------|
| 状态下拉框 | 可选：INIT / VALID / EXPIRED / REFRESHING / RISK / DISABLED，支持 `clearable` |
| 关键字搜索框 | 实时过滤 task_name、task_code、channel、mobile_phone、account_alias、profile_key、script_name、script_code |

### 表格列

| 列 | 说明 |
|----|------|
| 任务名称 | `task_name` |
| 运行频率 | `schedule_value \|\| schedule_type` |
| 渠道 | `channel` |
| 手机号 / 账号 | `mobile_phone` + `account_alias` |
| 涉及 DNS | `related_dns.join(", ")` |
| Profile Key | `profile_key` |
| Profile 相对路径 | `profile_relative_path` |
| 维护脚本 | `script_name` + `script_code · script_type · platform` |
| 状态 | 带颜色标签：VALID 绿色 / RISK 红色 / EXPIRED 红色 / REFRESHING 黄色 / 其余灰色 |
| 最近执行时间 | `last_run_at` 或「尚未执行」 |
| 操作 | 见下方表格 |

### 操作按钮

| 按钮 | 前提条件 | 行为 |
|------|----------|------|
| **详情** | 无 | 打开 `TaskDetailDialog`，展示任务完整信息 |
| **编辑** | 无 | 打开 `TaskFormDialog`（回填数据），提交后 `PATCH /api/tasks/{code}` |
| **执行脚本** | 任务必须处于启用状态 (`enabled=true`)，否则按钮 `disabled` | 调用 `POST /api/tasks/{code}/execute`，显示 loading，完成后弹出结果消息并刷新列表 |
| **打开修复浏览器** | 当前任务有 status 非 CLOSED 的工单，否则弹出警告 | 调用 `POST /api/repairs/{ticket_code}/open`，刷新列表 |
| **停用 / 启用** | 无 | 调用 `POST /api/tasks/{code}/toggle`，显示对应成功消息并刷新列表 |

### 查询参数

- `?create=1` — 自动打开新建对话框（用于外部跳转）

### 对话框：TaskDetailDialog

只读展示任务的全部字段，包含：

| 区域 | 内容 |
|------|------|
| 基础信息 | 任务名称、编码、运行频率、渠道 |
| 账号信息 | 手机号、账号别名 |
| DNS 信息 | DNS 列表 |
| 脚本信息 | 脚本名称、编码、类型、平台 |
| Profile 信息 | Profile Key、相对路径、绝对路径 |
| 状态信息 | 状态标签、最近执行时间、最近运行状态、最近运行消息 |

对话框底部有 **执行脚本** 按钮（同列表行为）和 **关闭** 按钮。

### 对话框：TaskFormDialog

创建/编辑共用的表单对话框，标题自动切换「新增维护任务」/「编辑维护任务」。

| 字段 | 类型 | 说明 |
|------|------|------|
| 任务名称 | text input | 必填 |
| 运行频率类型 | select | cron / interval / once |
| 运行频率值 | text input | 根据类型填写 cron 表达式或间隔秒数 |
| 渠道 | select | KUAISHOU / TAOBAO / TMALL / ALIMAMA / JD / PDD |
| 手机号 | text input | 必填 |
| 账号别名 | text input | 可选 |
| DNS | tag input | 可输入多个 DNS，回车添加 |
| 关联脚本 | select | 从脚本库列表选择 |
| Profile Key | select | 从 Profile 目录列表选择 |
| 预期状态 | text input | 可选，执行后预期结果状态 |
| 预期消息 | text input | 可选，执行后预期结果消息 |

**按钮**：提交（带 loading）/ 取消

---

## 3. 健康检测 HealthChecks

**数据加载**：页面加载时自动请求 `GET /api/health-checks`、`GET /api/tasks`。

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 检测总数 | 全部检测数 |
| 通过 | `status === "PASS"` 的检测数 |
| 失败 | `status === "FAIL"` 的检测数 |

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **执行全部健康检测** | 调用 `POST /api/health-checks/execute-all`，显示 loading，完成后弹出「已执行 N 条健康检测」并刷新列表 |
| **新增健康检测** | 打开 `HealthCheckFormDialog`（空白表单），提交后 `POST /api/health-checks` |

### 筛选栏

| 控件 | 说明 |
|------|------|
| 状态下拉框 | 可选：PENDING / PASS / FAIL / DISABLED |
| 关键字搜索框 | 实时过滤 check_name、check_code、channel、shop_name、mobile_phone、dns、check_url、trigger_task_code |

### 表格列

| 列 | 说明 |
|----|------|
| 检测名称 | `check_name` |
| cookie 表 | `cookie_table` |
| 定位信息 | `channel / shop_name` + `mobile_phone / dns` |
| 检测 API | `method` + `check_url` |
| 成功规则 | `success_rule` 以 JSON 格式展示 |
| 失败规则 | `failure_rule` 以 JSON 格式展示 |
| 绑定任务 | `trigger_task_code` 或「未绑定任务」 |
| 状态 | 带颜色标签：PASS 绿色 / FAIL 红色 / PENDING 黄色 / DISABLED 灰色 |
| 最近检测时间 | `last_checked_at` 或「尚未执行」 |
| 操作 | 见下方表格 |

### 操作按钮

| 按钮 | 前提条件 | 行为 |
|------|----------|------|
| **编辑** | 无 | 打开 `HealthCheckFormDialog`（回填），提交后 `PATCH /api/health-checks/{code}` |
| **执行检测** | 必须启用 (`enabled=true`)，否则 `disabled` | `POST /api/health-checks/{code}/execute`，完成后弹出结果并刷新 |
| **停用 / 启用** | 无 | `POST /api/health-checks/{code}/toggle` |

### 查询参数

- `?create=1` — 自动打开新建对话框
- `?runAll=1` — 自动触发「执行全部健康检测」

### 对话框：HealthCheckFormDialog

| 字段 | 类型 | 说明 |
|------|------|------|
| 检测名称 | text input | 必填 |
| Cookie 表名 | text input | 必填 |
| 渠道 | select | KUAISHOU / TAOBAO / TMALL / ALIMAMA / JD / PDD |
| 店铺名称 | text input | 可选 |
| 手机号 | text input | 可选 |
| DNS | text input | 可选 |
| API 请求方法 | select | GET / POST |
| 检测 URL | text input | 必填 |
| 成功规则 | JSON 编辑 | 可选，形如 `{"json_path": "...", "expected": "..."}` |
| 失败规则 | JSON 编辑 | 可选，形如同上 |
| 触发任务 | select | 必填，从维护任务列表选择 |

**按钮**：提交（带 loading）/ 取消

---

## 4. 脚本库 Scripts

**数据加载**：页面加载时自动请求 `GET /api/scripts`、`GET /api/profiles`。

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 脚本总数 | 全部脚本数 |
| 启用中 | `enabled === true` 的脚本数 |

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **上传脚本** | 打开 `ScriptUploadDialog` |

### 表格列

| 列 | 说明 |
|----|------|
| 脚本名称 | `script_name` |
| 类型 | `script_type`：MAINTAIN / HEALTH / MANUAL / CUSTOM |
| 平台 | `platform`：COMMON / KUAISHOU / TAOBAO / TMALL / ALIMAMA / JD / PDD |
| 选择目录 | `<el-select>` 下拉框，列出所有已登记 Profile 的 profile_key，可清空。变更时 `PATCH /api/scripts/{code}/profile` |
| 目录与主文件 | `script_dir` + `absolute_dir\main_file` |
| 说明 | `description` 或「未填写说明」 |
| 操作 | 见下方表格 |

### 操作按钮

| 按钮 | 行为 |
|------|------|
| **详情** | 打开 `ScriptDetailDialog` 展示脚本完整信息 |
| **停用 / 启用** | `POST /api/scripts/{code}/toggle`，刷新列表 |

### 查询参数

- `?upload=1` — 自动打开上传对话框

### 对话框：ScriptUploadDialog

| 字段 | 类型 | 说明 |
|------|------|------|
| 脚本名称 | text input | 必填 |
| 脚本类型 | select | MAINTAIN / HEALTH / MANUAL / CUSTOM |
| 平台 | select | COMMON / KUAISHOU / TAOBAO / TMALL / ALIMAMA / JD / PDD |
| 说明 | textarea | 可选 |
| 选择脚本文件 | file input | `.py` 文件，必选 |

**规则提示**：仅支持上传单个 `.py` 脚本文件。脚本编码和版本由系统自动管理（后端自动生成 `script_code` 并管理版本目录）。

**按钮**：上传脚本（必填验证通过才可点击，带 loading）/ 取消

### 对话框：ScriptDetailDialog

只读展示脚本信息：

| 区域 | 内容 |
|------|------|
| 基础信息 | 脚本名称、类型/平台、启用状态、更新时间 |
| 路径信息 | 脚本目录、绝对目录、主文件 |
| 说明 | description 或「未填写说明」 |

**按钮**：关闭

---

## 5. Profile 目录 Profiles

**数据加载**：页面加载时自动请求 `GET /api/profiles`、`GET /api/tasks`。

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **登记 Profile** | 打开 `ProfileFormDialog` |

### 表格列

| 列 | 说明 |
|----|------|
| Profile Key | `profile_key` |
| 绑定任务 | 查找 `tasks` 中 `task_id === profile.task_id` 或 `profile_key === profile.profile_key` 的任务名 + 任务编码，找不到显示「未绑定任务」 |
| 路径预览 | `relative_path`（code 样式）+ `absolute_path` |

### 对话框：ProfileFormDialog

| 字段 | 类型 | 说明 |
|------|------|------|
| Profile Key | text input | 必填，唯一标识 |
| 目录路径 | text input + **浏览...** 按钮 | 必填。支持相对路径（`runtime/profiles/…`）或 Windows 绝对路径（`D:\path\to\profile`）。点击「浏览...」打开 `FolderBrowserDialog` 选择目录后自动回填 |
| 任务绑定 | select | 可选，从维护任务列表选择 |

**浏览... 按钮行为**：打开 `FolderBrowserDialog`，初始加载 Windows 驱动器列表，双击驱动器进入目录，双击子目录继续导航，选中后点击「选择目录」回填路径。

**按钮**：提交 / 取消

---

## 6. 人工修复 Repairs

**数据加载**：页面加载时自动请求 `GET /api/repairs`。

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 待处理工单 | status 为 OPEN / WAIT_RDP_REPAIR / BROWSER_OPENED / VERIFYING / FAILED 的工单 |
| 已打开浏览器 | status 为 BROWSER_OPENED 的工单 |
| 已关闭工单 | status 为 CLOSED 的工单 |

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **刷新工单** | 重新请求 `GET /api/repairs` |

### 表格列

| 列 | 说明 |
|----|------|
| 工单号 | `ticket_code` |
| 任务 / Profile | `task_name` + `profile_key` |
| 风险信息 | `risk_type` + `risk_message` |
| 状态 | 带颜色标签：CLOSED 绿色 / BROWSER_OPENED+VERIFYING 黄色 / FAILED 红色 / OPEN+WAIT_RDP_REPAIR 灰色 |
| 处理记录 | `repaired_by` + `browser_opened_at` + `closed_at` |
| 操作 | **处理工单** 按钮 |

### 操作按钮

| 按钮 | 行为 |
|------|------|
| **处理工单** | 打开 `RepairGuideDialog` |

### 对话框：RepairGuideDialog

引导用户完成人工修复三步流程：

**步骤说明**（带对应按钮）：

| 步骤 | 按钮 | 行为 |
|------|------|------|
| ① 通过 RDP 登录部署机桌面会话 | — | 仅文案提示 |
| ② 打开修复浏览器 | **打开修复浏览器** | `POST /api/repairs/{ticket_code}/open`，传入修复人。后端打开浏览器并更新工单状态为 BROWSER_OPENED |
| ③ 在浏览器中完成人工操作 | — | 仅文案提示（登录、验证、提取 cookie 等） |
| ④ 回到系统触发复检 | **执行复检** | `POST /api/repairs/{ticket_code}/verify`，执行检测并更新工单 |
| ⑤ 关闭工单（可选） | **关闭工单** | 弹出 confirm 确认框提示「关闭工单不会恢复任务为可用状态」，确认后 `POST /api/repairs/{ticket_code}/close` |

额外展示信息：
- 当前工单状态标签
- **Profile 目录路径**（绝对路径），方便用户知道浏览器使用的 profile 位置

**按钮**：关闭

---

## 7. 运行日志 Logs

### 筛选栏

**两行布局**，每行 4 列（响应式窄屏变为 1 列）：

| 控件 | 说明 |
|------|------|
| 运行类型下拉 | TASK / CHECK / REPAIR / SCRIPT / PROFILE / ENV / SYSTEM |
| 状态下拉 | SUCCESS / PASS / FAIL / FAILED / RISK / WARN / RUNNING |
| 任务 ID | 文本输入，精确匹配 |
| 检测 ID | 文本输入，精确匹配 |
| 工单 ID | 文本输入，精确匹配 |
| 关键字 | 模糊匹配 title / message |
| 开始时间 | ISO 格式，如 `2026-07-01T00:00:00` |
| 结束时间 | ISO 格式，如 `2026-07-01T23:59:59` |

### 按钮

| 按钮 | 行为 |
|------|------|
| **筛选日志** | 以当前 filters 调用 `GET /api/logs` |
| **重置** | 清空全部筛选条件，自动重新加载全部日志 |

### 表格列

| 列 | 说明 |
|----|------|
| 时间 | `created_at` |
| 类型 | `run_type` |
| 关联对象 | 任务 ID + 检测 ID + 工单 ID（不存在显示 `-`） |
| 状态 | 带颜色标签 |
| 标题 | `title` |
| 消息 | `message` |
| 日志文件 | `log_file_path` 或「未落盘」 |
| 操作 | **详情** 按钮 |

### 操作按钮

| 按钮 | 行为 |
|------|------|
| **详情** | 打开 `LogDetailDialog`，展示运行日志完整信息 |

### 对话框：LogDetailDialog

只读展示日志的全部字段：

| 区域 | 内容 |
|------|------|
| 基本信息 | 时间、类型、状态、标题、消息 |
| 关联信息 | 任务 ID、检测 ID、工单 ID |
| 产物目录 | artifact_dir |

**按钮**：关闭

---

## 8. 环境自检 Environment

### 统计卡片

| 卡片 | 含义 |
|------|------|
| 检查项总数 | 全部检查项数 |
| 警告项 | status 为 WARN 的项 |
| 失败项 | status 为 FAIL 的项 |

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **查看最近结果** | `GET /api/environment/latest` 加载最近一次自检结果 |
| **执行环境自检** | `POST /api/environment/execute`，重新执行全部检查并展示结果 |

### 检查项卡片

每项检查以卡片展示：check_code / status 标签 / summary / created_at。
卡片左侧带颜色边框：PASS 绿色 / WARN 黄色 / FAIL 红色。

空列表时展示引导文案。

### 部署提示

底部固定 `<el-alert>`：提醒桌面会话为 WARN 时应通过 RDP 登录部署机。

### 查询参数

- `?autorun=1` — 自动执行环境自检

---

## 9. 部署配置 Deploy

**只读展示**，不提供编辑功能。

### 工具栏按钮

| 按钮 | 行为 |
|------|------|
| **刷新配置** | 重新请求 `GET /api/deploy` |

### 当前用户提示

`<el-alert type="warning">`，显示 `current_user` 和 `current_user_hint`。

### 配置卡片（2×2 网格）

| 卡片 | 内容 |
|------|------|
| 部署根目录 | `deploy_root` |
| 运行根目录 | `runtime_root` |
| 启动命令 | `startup_command` |
| API 监听 | `api_host:api_port` |

### 关键目录列表

`directories` 字典以 `key → value` 形式列出。

---

## 通用 UI 模式

### 页面布局

- 所有页面使用 `.page-grid`（`display: grid; gap: 16px`）
- 每个功能模块内嵌 `.panel` 卡片容器（border + radius + shadow + surface 背景）
- `.panel-header` 内部 flex 布局，左侧标题+说明，右侧操作按钮

### 空状态

所有 `<el-table>` 通过 `empty-text` 或 `<el-empty>` 组件展示引导文案，告诉用户当前没有数据时应该做什么。

### Loading 状态

- 表格通过 `v-loading` 绑定 loading 状态
- 操作按钮通过 `:loading` 绑定对应操作的 loading 变量，防止重复点击

### 错误处理

所有异步操作通过 try/catch 捕获，统一使用 `ElMessage.error` 展示错误信息，优先使用 `error.message`，兜底使用中文默认文案。

### 筛选状态

- 维护任务、健康检测、运行日志页有客户端或服务端筛选
- Vue `computed` + `filter()` 实现客户端实时过滤（任务、检测）
- 运行日志通过提交 filters 到服务端过滤
