# 实施计划

## 第一阶段：数据库任务配置

状态：已完成

- `cookie_tasks` 保存前端任务配置和最近状态。
- `crawler_cookies` 按站点、账号去重保存 Cookie。
- `task_runs` 保存每次探测和刷新记录。

## 第二阶段：交互管理后台

状态：已完成

- 新增、编辑和删除任务。
- 修改账号、密码、网址和脚本。
- 启用、停用定时探测。
- 搜索和状态筛选。
- Cookie 数据和运行记录页面。

## 第三阶段：后台执行

状态：已完成

- 手动立即探测。
- 手动立即刷新。
- 定时探测失败后自动刷新。
- 页面每 5 秒自动同步状态。
- 同一任务禁止并发重复执行。

## 第四阶段：验证

状态：已完成

- 单元测试、静态编译和依赖检查。
- 使用模拟数据进行桌面端页面和交互验证。

## 真实环境联调

状态：待用户创建 MySQL 表后执行

- 填写 `.env` 数据库连接。
- 在前端登记 PDD 任务。
- 验证真实 Cookie 探测。
- 验证 Edge/CDP 登录和两份 Cookie 入库。
